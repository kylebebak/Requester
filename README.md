# Sublime HTTP Requests
An Sublime Text HTTP client built on top of the wonderful [Requests](http://docs.python-requests.org/en/master/) library, which means you're probably familiar with the syntax.
